<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/thread_holder"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/spacing_small"
    android:background="@color/chat_background">


    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/conversation_appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/conversation_toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    </com.google.android.material.appbar.AppBarLayout>


        <!-- messages list -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/thread_messages_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_above="@+id/message_divider"
            android:layout_below="@+id/conversation_appbar"
            android:overScrollMode="ifContentScrolls"
            android:clipToPadding="false"
            android:paddingTop="@dimen/activity_margin"
            android:paddingBottom="@dimen/activity_margin"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:stackFromEnd="true" />
        <LinearLayout
            android:id="@+id/thread_messages_empty_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:layout_above="@+id/message_divider"
            android:layout_below="@+id/conversation_appbar"
            android:visibility="gone">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/font_large"
                android:text="@string/empty_chat_msg"
                android:gravity="center_horizontal"/>

        </LinearLayout>


        <!-- send message layout -->

        <View
            android:id="@+id/message_divider"
            android:layout_width="match_parent"
            android:layout_height="1px"
            android:layout_above="@+id/thread_attachments_holder"
            android:background="@color/divider"
            android:importantForAccessibility="no" />

        <ImageView
            android:id="@+id/thread_add_attachment"
            android:layout_width="@dimen/normal_icon_size"
            android:layout_height="@dimen/normal_icon_size"
            android:layout_alignParentStart="true"
            android:layout_alignParentBottom="true"
            android:layout_marginTop="@dimen/spacing_small"
            android:layout_marginEnd="@dimen/spacing_small"
            android:alpha="0.9"
            android:background="?selectableItemBackgroundBorderless"
            android:padding="@dimen/spacing_normal"
            android:src="@drawable/ic_attach_file_white" />


        <!-- attachment list -->
        <HorizontalScrollView
            android:id="@+id/thread_attachments_holder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_above="@+id/conversation_type_message"
            android:layout_alignStart="@+id/conversation_type_message"
            android:layout_marginTop="@dimen/spacing_normal"
            android:layout_marginBottom="@dimen/spacing_small"
            android:overScrollMode="never"
            android:scrollbars="none"
            android:visibility="gone">

            <LinearLayout
                android:id="@+id/thread_attachments_wrapper"
                android:layout_width="wrap_content"
                android:layout_height="@dimen/attachment_preview_size"
                android:divider="@drawable/linear_layout_divider"
                android:orientation="horizontal"
                android:paddingEnd="@dimen/spacing_small"
                android:showDividers="middle" />
        </HorizontalScrollView>


        <!-- message field -->

        <EditText
            android:id="@+id/conversation_type_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_marginEnd="@dimen/spacing_small"
            android:layout_toEndOf="@+id/thread_add_attachment"
            android:padding="@dimen/spacing_normal"
            android:background="@android:color/transparent"
            android:gravity="center_vertical"
            android:hint="Type a Message"
            android:inputType="textCapSentences|textMultiLine"
            android:minHeight="@dimen/normal_icon_size"
            android:layout_toStartOf="@id/chatroom_send_message_fab"/>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/chatroom_send_message_fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:layout_margin="5dp"
            app:fabSize="mini"
            android:src="@drawable/ic_send_white" />
</RelativeLayout>
